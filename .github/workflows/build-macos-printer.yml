name: Build FTS Printer macOS

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'macos-app/**'
      - '.github/workflows/build-macos-printer.yml'

permissions:
  contents: write

jobs:
  build:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4
      - name: Build universal macOS app
        run: |
          chmod +x macos-app/build.sh
          macos-app/build.sh
      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: FTS-Printer-macOS
          path: |
            dist/FTS-Printer-macOS.zip
            dist/FTS-Printer-macOS.dmg
          if-no-files-found: error
      - name: Publish downloadable release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          TAG="fts-printer-mac-v0.1.2"
          if gh release view "$TAG" >/dev/null 2>&1; then
            gh release upload "$TAG" dist/FTS-Printer-macOS.zip dist/FTS-Printer-macOS.dmg --clobber
          else
            gh release create "$TAG" dist/FTS-Printer-macOS.zip dist/FTS-Printer-macOS.dmg               --target "$GITHUB_SHA"               --title "FTS Printer macOS v0.1.2"               --notes "Erste installierbare FTS Printer Version für MacBook und iMac. Native Mitarbeiter-Anmeldung, Selfie-Druckaufträge, Kundenbelege, Materialbestand, lokaler Eventordner und automatische Kartenimporte mit SHA-256-Dublettenprüfung."
          fi
